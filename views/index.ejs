<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GECA News Updates</title>

    <!-- ✅ Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
      /* 🌈 Animated Gradient Background */
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        z-index: -1;
        background: linear-gradient(-45deg, #082b8a, #4b098a, #069c8d, #960606);
        background-size: 400% 400%;
        animation: gradient 13s ease infinite;
      }

      @keyframes gradient {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }

      .news-ticker {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(4px);
        overflow: hidden;
        white-space: nowrap;
        border-top: 1px solid rgba(255, 255, 255, 0.12);
        border-bottom: 1px solid rgba(255, 255, 255, 0.12);
      }

      .ticker-slide {
        display: inline-block;
        padding-left: 100%;
        animation: ticker 80s linear infinite;
      }

      .news-ticker:hover .ticker-slide {
        animation-play-state: paused;
      }

      @keyframes ticker {
        0% {
          transform: translateX(0);
        }
        100% {
          transform: translateX(-100%);
        }
      }

      .ticker-item {
        display: inline-block;
        padding: 0 2rem;
      }

      @keyframes trainMove {
        0% {
          transform: translateX(0) scale(1);
          opacity: 1;
        }
        50% {
          transform: translateX(150px) scale(1.1);
        }
        100% {
          transform: translateX(1000px) scale(0.95);
          opacity: 0;
        }
      }

      .animate-train {
        animation: trainMove 0.6s ease-out forwards;
      }
    </style>
  </head>
  <body
    class="overflow-x-hidden min-h-screen text-white flex flex-col justify-center items-center px-4"
  >
    <!-- 🔥 Logo -->
    <img
      src="/logo.jpg"
      alt="GECA Logo"
      class="w-32 md:w-40 mb-2 rounded-xl shadow-2xl transition-transform hover:scale-105"
    />

    <!-- 🔥 Heading -->
    <h3 class="text-lg md:text-3xl text-center mb-4">
      Want to stay updated?<br />
      Subscribe to <strong>GECA News Updates</strong> for the latest news!
    </h3>

    <!-- 🔥 News Ticker -->
    <div class="news-ticker w-full py-2 mb-6">
      <% if (news && news.length) { %>
      <div class="ticker-slide">
        <% news.forEach(function (item, index) { %>
        <span class="ticker-item">
          <a
            href="<%= item.link %>"
            target="_blank"
            rel="noopener"
            class="text-yellow-300 hover:underline font-medium"
            ><%= item.title %></a
          >
          <% if (index < news.length - 1) { %> | <% } %>
        </span>
        <% }); %>
      </div>
      <% } else { %>
      <div class="p-2 text-center text-gray-300">No news to display.</div>
      <% } %>
    </div>

    <!-- 🔥 Message -->
    <% if (typeof message !== "undefined" && message) { %>
    <div
      id="server-message"
      class="mb-4 px-4 py-2 rounded bg-green-600 text-white text-center font-semibold shadow"
    >
      <%= message %>
    </div>
    <% } %>

    <!-- 🔥 Subscribe Button -->
    <a href="/auth/google" id="subscribe-link">
      <button
        id="subscribe-btn"
        class="flex items-center gap-2 px-6 py-2 text-white font-semibold rounded-full border border-white bg-transparent shadow-lg transition-transform relative"
      >
        <img
          src="https://www.svgrepo.com/show/475656/google-color.svg"
          alt="Google"
          class="w-5 h-5"
        />
        <span id="btn-text">Subscribe with Google</span>
        <span
          id="train-trail"
          class="absolute left-0 top-0 w-full h-full bg-white opacity-10 blur-2xl scale-0 transition-transform duration-300"
        ></span>
      </button>
    </a>

    <!-- <script>
      const btn = document.getElementById("subscribe-btn");
      const link = document.getElementById("subscribe-link");

      btn.addEventListener("click", function (e) {
        e.preventDefault(); // Prevent immediate navigation
        btn.classList.add("animate-train");
        setTimeout(() => {
          window.location.href = link.href;
        }, 600);
      });
    </script> -->

    <!-- 🔥 Benefits -->
    <div
      class="grid grid-cols-1 md:grid-cols-2 gap-4 max-w-4xl mt-2 text-gray-200"
    >
      <div class="bg-gray-800 rounded-2xl p-4 shadow-lg border border-gray-700">
        <h4 class="text-md font-bold mb-1">📢 Instant Alerts</h4>
        <p class="text-sm">
          Get notified as soon as a new announcement is posted.
        </p>
      </div>
      <div class="bg-gray-800 rounded-2xl p-4 shadow-lg border border-gray-700">
        <h4 class="text-md font-bold mb-1">📅 Never Miss a Notice</h4>
        <p class="text-sm">Stay ahead of deadlines and events from GECA.</p>
      </div>
      <div class="bg-gray-800 rounded-2xl p-4 shadow-lg border border-gray-700">
        <h4 class="text-md font-bold mb-1">🛡️ Spam-Free Guarantee</h4>
        <p class="text-sm">Only relevant updates. No spam, ever.</p>
      </div>
      <div class="bg-gray-800 rounded-2xl p-4 shadow-lg border border-gray-700">
        <h4 class="text-md font-bold mb-1">📬 Simple Unsubscribe</h4>
        <p class="text-sm">You’re in control. Leave anytime with one click.</p>
      </div>
    </div>
  </body>
</html>
